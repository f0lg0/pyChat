
================== ToDo ==================
	place a "x" inside [] if done

1. [] Use fixed length headers
2. [x] Remove "self." to "to_send_msg" in "sendMsg()"
3. [x] Implement a nicer way of handling the flags
4. [x] Use argparse to specify options
5. [x] Replace open() with "with open()"
6. [x] Use objects in the messages to get rid of flags and bracketed commands
7. [x] Re-add user input as a second option
8. [x] Prevent server from crashing if the users close the terminal
9. [] Choose a way to handle streaming 

# ======= NOTE ========
argparse lets you specify input in the terminal windows like:

$ python3 client.py <server_ip> <server_port>

for example:

$ python3 client.py 192.168.1.100 1234

so we can remove user input inside the code, i think it's better but not essential.
I mainly thought about it because it just looks cooler haha


================= Server errors if stoopid windowz closes terminal with the x button =======================================

data = client_socket.recv(self.BUFFER_SIZE) 
	ConnectionResetError: [Errno 104] Connection reset by peer

self._target(*self._args, **self._kwargs)  
	BrokenPipeError: [Errno 32] Broken pipe   
